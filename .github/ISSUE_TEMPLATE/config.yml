# Disables the "Open a blank issue" link at the bottom of the picker, so
# users see only the three structured templates (bug, feature, bang
# suggestion). Keeps unstructured noise out of the issue tracker.
blank_issues_enabled: false
